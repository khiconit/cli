#How to build the CLI
```bash
pnpm install
npm run bundle-for-release
```

Cd to the cli package and run the following command:
```bash
cd packages/cli
pnpm pack
```

Commit the changes and push to the remote repository.
